[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
addopts = -v --tb=short
markers =
    window: opens real Hariku windows; skipped unless HARIKU_UI_TESTS=1 (GitHub Actions sets it)
