# ── Python ──────────────────────────────────────────────
venv/
__pycache__/
*.py[cod]
.pytest_cache/

# ── Build / compile outputs (regenerated by CI) ─────────
hariku.build/
hariku.dist/
Output/
Output-update/
*.spec

# ── Local runtime / scratch / caches ────────────────────
scratchpad/
extensions/.cache/
data/
mock_store/
store_release/

# ── Secrets & old-backend copy (NEVER publish) ──────────
# Full copy of the retired novarealm backend: DB dumps, connect.php creds, etc.
hariku.novarealm.cloud/
.env
*.env
settings/*.local.json

# ── Misc build artifacts ────────────────────────────────
Hariku_V2_Developer_SDK.zip
*.log

# ── Personal notes and retired files (kept locally, not part of the project) ──
/how-to.txt
/news.txt
/body.html
/sdk_release_post.md
/hariku_v2_journey.md
# Old novarealm-era update files; CI now generates version.json per release.
/version.json
/update.iss
# Old copy of the store listing; the app reads the live one from GitHub Pages.
/registry.json
